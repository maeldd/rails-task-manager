<div class="container">
  <div>
    <h2><%= @task.content %></h2>
    <p><%= @task.created_at %></p>
    <p><%= @task.done %></p>
  </div>
  <div>
    <%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
  </div>
</div>
